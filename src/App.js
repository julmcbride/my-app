// components
import DropdownList from './components/DropdownList';

function App() {
  return (
    <div className="App">
      <header className="App-header" style={{margin: '2rem auto', textAlign: 'center'}}>
        <DropdownList/>
      </header>
    </div>
  );
}

export default App;
